version: 1

sitemaps:
  blog:
    origin: https://www.diffa.co.uk
    lastmod: YYYY-MM-DD
    languages:
      default:
        source: /query-index.json
        destination: /sitemap.xml
        hreflang: en
        alternate: /{path}
